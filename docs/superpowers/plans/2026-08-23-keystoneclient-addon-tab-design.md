# KeystoneClient Addon Tab Plan

1. Map the approved references to `AddonStatus` and `WowState` without demo state.
2. Replace only the legacy Addon content wrapper and implement the two-column view.
3. Connect folder, check, install, update and reinstall controls to existing wrappers.
4. Extend unit tests for state rendering and typed actions.
5. Add Playwright baselines for installed and not-installed Addon views.
6. Run frontend validation, inspect screenshots and classify deployment impact.
