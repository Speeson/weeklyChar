# KeystoneClient Poison Production Artwork Plan

1. Add failing registry tests for Poison asset resolution, isolation, semantic icons, navigation, profile, fallback, and invalid theme behavior.
2. Copy the supplied PNG bytes from the main checkout into the feature worktree, correcting only the two source filename typos at the destination, and verify hashes.
3. Register all runtime artwork centrally and integrate the Poison background and subtle ambient overlay.
4. Compose shell branding, tabs, profile/avatar, window controls, and footer without changing interactive targets.
5. Compose Synchronization summary, table, emblem/version/status sidebar, status icons, and action artwork without changing state or table behavior.
6. Apply shared frame normalization and reusable 9-slice chrome to variable-size Poison overlays.
7. Run focused tests, create canonical human-review screenshots, compare them with the north star, and refine layout without touching goldens.
8. Run independent self-review, complete frontend/Rust/release/deployment validation, commit scoped changes, and stop for owner visual review.
