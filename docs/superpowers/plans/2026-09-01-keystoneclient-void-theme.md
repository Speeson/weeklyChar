# KeystoneClient Void Theme Plan

1. Audit the checked-out `main` theme registry, full token contract, shared shell/pages, supplied Void asset paths and dimensions, unit coverage, preview routes, and existing golden snapshots.
2. Add failing tests for the selectable `void` ID, live selection and reload persistence, exact semantic asset mapping, `overlay1` production default, overlay2/3 comparison roles, active-only tab decoration, Void dropdown/web/tray roles, and absence of `empty-button.png` from runtime imports.
3. Register Void centrally, extend only generic optional overlay roles, and import a complete `void.css` token implementation without modifying Keystone or Poison values.
4. Refine Void asset sizing and shared CSS composition for the shell, Sync, Addon, Teams, authentication, onboarding, Settings, dropdowns, dialogs, and modals while preserving existing markup and behavior.
5. Add deterministic Void Playwright coverage for the requested global surfaces and a review-only overlay1/2/3 comparison that temporarily overrides the document artwork slot without creating production behavior.
6. Generate only new Void snapshots, inspect every image, and compare pre/post hashes for all existing Keystone and Poison goldens.
7. Add the required pending Client changeset, review the full diff, run frontend, visual, Rust, release, deployment-impact, and hygiene validation, then write the SDD final report with remaining limitations.
