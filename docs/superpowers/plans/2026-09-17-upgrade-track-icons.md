# Upgrade Track Icons Plan

1. Add a bounded optional loot favorite `upgradeTrack` value at the exact variant source and pass it through Worker objective, selector, and planner projections.
2. Restore missing Client tracks from verified Midnight Season 2 exact-variant bonus-list IDs while older published API responses remain in use.
3. Add a small Client and Web upgrade-track icon renderer, with unknown tracks omitted.
4. Decorate Client equipment and loot objective images, Web objective images, and all Client/Web Great Vault reward labels.
5. Validate contracts, builds, relevant visual behavior, and deployment impact; review the diff.
